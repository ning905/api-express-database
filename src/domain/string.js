const errorMessage = {
  bookIdNotFound: "A book with the provided ID was not found",
  bookTitleExists: "A book with the provided title already exists",
  missingField: "Missing fields in the request body",
};

module.exports = { errorMessage };
